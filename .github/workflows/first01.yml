name: First-wf
on: workflow_dispatch
jobs:
  firstjob:
    runs-on: ubuntu-latest
    steps:
        - name: print greeting
          run: echo "Hello world"
        - name: goodbye
          run: echo "Done-bye
        - name: installing httpd
          run: yum install -y

  secondjob:
    runs-on: ubuntu-latest
    steps:
        - name: install apps
          run: install tree -y
